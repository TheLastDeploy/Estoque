<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard de Estoque da mariecarie</title>
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: #2e2e2e;
            color: white;
            display: flex;
        }

        .sidebar {
            width: 70px;
            background-color: #111;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: space-between;
            padding: 15px 0;
        }

        .sidebar i {
            font-size: 22px;
            margin: 15px 0;
            cursor: pointer;
        }

        .sidebar small {
            font-size: 10px;
            margin-bottom: 10px;
        }

        .main {
            flex: 1;
            padding: 30px;
        }

        h1 {
            margin-top: 0;
        }

        h2 {
            margin-top: 40px;
        }

        .cards {
            display: flex;
            gap: 30px;
            margin-top: 20px;
        }

        .card {
            flex: 1;
            background-color: #3a3a3a;
            border: 2px solid #000;
            border-radius: 10px;
            padding: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: space-between;
        }

        .chart {
            width: 100%;
            height: 150px;
            display: flex;
            align-items: flex-end;
            justify-content: space-around;
        }

        .bar {
            width: 30px;
            background-color: #ccc;
            border-radius: 5px;
        }

        .bar:nth-child(1) {
            height: 90px;
        }

        .bar:nth-child(2) {
            height: 130px;
        }

        button {
            background-color: #222;
            color: white;
            border: none;
            padding: 8px 15px;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 10px;
        }

        button:hover {
            background-color: #333;
        }
    </style>
</head>
<body>
    <div class="sidebar">
            <i>➕</i>
            <small>Cadastrar</small>
            <i>👤</i>
        </div>
    </div>

    <div class="main">
        <h1>DashBoard</h1>
        <h2>Estoque de Produtos</h2>

        <div class="cards">
            <div class="card">
                <div class="chart">
                    <div class="bar"></div>
                    <div class="bar"></div>
                </div>
                <button>Mais detalhes</button>
            </div>

            <div class="card">
                <div class="chart">
                    <div class="bar"></div>
                    <div class="bar"></div>
                </div>
                <button>Mais detalhes</button>
            </div>
        </div>
    </div>
</body>
</html>
